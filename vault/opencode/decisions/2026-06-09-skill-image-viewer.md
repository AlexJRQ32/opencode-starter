# Skill image-viewer creada

Creada skill `image-viewer` en `~/.agents/skills/image-viewer/` para analizar im?genes que el usuario especifique por ruta. Usa Python/Pillow para extraer dimensiones, paleta de colores, brillo, cuadrantes y detecci?n de texto. Se activa con frases como "mira esta imagen", "ve esta imagen", "analiza esta imagen", o al pasar una ruta de imagen.

- `SKILL.md`: instrucciones y flujo de trabajo
- `image_view.py`: script de an?lisis Python que devuelve JSON estructurado

- Relacionado: [[2026-06]]
