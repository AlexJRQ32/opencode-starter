# opencode-starter y skills

## Contexto
- Se cre? repo `opencode-starter` en GitHub (AlexJRQ32/opencode-starter)
- Contiene SETUP.md como prompt de implementaci?n, AGENTS.md con reglas endurecidas, y README.md con gu?a
- Se copiaron 12 skills al repo en `skills/`

## Skills incluidos
1. `customize-opencode` ? config de opencode
2. `design-taste-frontend` ? anti-slop frontend
3. `emil-design-eng` ? filosof?a Emil Kowalski
4. `frontend-design` ? interfaces producci?n
5. `impeccable` ? pulido UI/UX
6. `model-router` ? dispatch autom?tico de modelos
7. `obsidian-vault` ? gesti?n vault
8. `token-efficiency` ? optimizaci?n tokens
9. `ui-ux-pro-max` ? inteligencia dise?o (13 CSVs + scripts Python)
10. `vercel-cli-with-tokens` ? deploy Vercel con tokens
11. `vercel-composition-patterns` ? patrones composici?n React (8 reglas)
12. `vercel-react-best-practices` ? performance React (60+ reglas)
13. `web-design-guidelines` ? auditor?a UI

## Pendiente
- Probar conexi?n Supabase MCP listando tablas de `Prueba-Supabase`
- Editar descripci?n del repo (usuario debe hacerlo manualmente desde GitHub)

- Relacionado: [[2026-06]]
