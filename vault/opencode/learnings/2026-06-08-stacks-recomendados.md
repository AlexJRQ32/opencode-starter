# Stacks y Workflows Recomendados para Estudiante

Basado en perfil: .NET + React ya dominados. Pr?ximos pasos para complementar.

## Stack #1: Mobile (alta empleabilidad)
- **React Native / Expo** ? ya sab?s React, es el salto m?s corto
- Alternativa: **.NET MAUI** ? si quer?s quedarte en ecosistema .NET
- Proyecto tipo: app para due?os de mascotas (complementa tu carn? veterinario)

## Stack #2: Cloud / DevOps profesional
- **Docker + Docker Compose** ? fundamental para cualquier deploy
- **GitHub Actions** (adem?s de Azure Pipelines) ? est?ndar de la industria
- **Terraform / Bicep** ? infraestructura como c?digo
- Ya ten?s Azure DevOps, sum? GitHub para portafolio

## Stack #3: Base de datos m?s all? de SQL Server
- **PostgreSQL** ? open source, lo usan casi todas las startups
- **MongoDB** ? NoSQL, complementa tu visi?n relacional
- **Redis** ? cach?, sesiones, colas (s?per valorado en entrevistas)

## Stack #4: Tiempo real y background jobs
- **SignalR** (ya lo ten?s en tu stack) ? profundiz?
- **RabbitMQ / Azure Service Bus** ? mensajer?a async
- **Hangfire / Quartz.NET** ? tareas programadas en .NET

## Stack #5: Testing y calidad
- **Cypress / Playwright** ? E2E testing (ya ten?s playwright instalado)
- **SonarCloud** ? calidad de c?digo (lo ten?s en tu plan original)
- **Storybook** ? desarrollo aislado de componentes React

## Stack #6: Fullstack moderno alternativo
- **Next.js 14+** (App Router, Server Components) ? React moderno, muy demandado
- **tRPC** ? APIs end-to-end type-safe, reemplaza REST en apps nuevas
- **Prisma ORM** ? alternativa moderna a EF Core si sal?s de .NET
- **TailwindCSS** ? ya lo ten?s ?

## Stack #7: Portfolio y presencia
- **Dominio propio** + p?gina personal con tus proyectos
- **GitHub** con READMEs buenos por proyecto
- **LinkedIn** optimizado + posts t?cnicos
- **Blog t?cnico** (dev.to, Medium, o propio con Next.js)

## Workflow recomendado para proyectos de estudiante

```
Idea ? GitHub repo (README + licencia)
     ? Plan r?pido en GitHub Projects (tablero)
     ? Docker Compose para dev environment
     ? CI: GitHub Actions (lint + test + build)
     ? Deploy: Vercel (front) + Railway/Render (back)
     ? Docs: README + Wiki + video demo corto
     ? Portfolio: link al repo + deploy + screenshot
```

## Orden sugerido para aprender
1. Docker (urgente ? lo piden en todos lados)
2. PostgreSQL (abre puertas a startups)
3. Next.js (evoluci?n natural de React)
4. GitHub Actions (ya sab?s Azure Pipelines, es similar)
5. React Native (si quer?s mobile)
6. Prisma / tRPC (si explor?s fuera de .NET)

---

- Relacionado: [[2026-06]]
